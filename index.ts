// @amlhubs/fipa-ip — public entry point
//
// Re-exports every interface and class declared in fipa-ip.ts so that
// downstream consumers can `import { InteractionProtocol, ... } from '@amlhubs/fipa-ip'`.

export * from './fipa-ip.js';
